language: android
android:
  components:
    - platform-tools
    - build-tools-23.0.3
    - android-23

    # Support library
    - extra-android-support
    - extra-android-m2repository
before_install:
 - chmod +x gradlew

script:
   - ./gradlew build connectedCheck
